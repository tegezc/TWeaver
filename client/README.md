# ThreatWeaver client

Vite + React app for ThreatWeaver. Product docs, WebMCP tools, and the judge path live in the [root README](../README.md).

```bash
npm install
npm run dev
```

Build: `npm run build` (output in `dist/`). Vercel Root Directory is this folder.
